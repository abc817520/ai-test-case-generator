Cursor Vibe Coding 完整分步实现路线（无代码版）
核心理念：小步快跑，每步必测；先跑通最小闭环，再逐步完善细节；全程让 Cursor 理解你的意图并帮你实现具体逻辑。
预计总耗时：3-4 小时纯编码时间
最终成果：一个可直接使用的、支持 docx/md 输入和 Excel 输出的多 Agent 测试用例生成系统
🚀 准备阶段（10 分钟）
步骤 1：环境搭建
创建项目文件夹并打开 VS Code/Cursor
创建 Python 虚拟环境并激活
一次性安装所有必需依赖包
创建.env文件，配置你的大模型 API 密钥和基础 URL
验收标准：在终端中运行python --version和pip list能看到正确的 Python 版本和所有已安装的包
Cursor 技巧：直接在终端中输入依赖列表，让 Cursor 帮你生成完整的 pip 安装命令
步骤 2：创建项目文件结构
创建以下 6 个空文件，保持结构清晰：
main.py：LangGraph 核心流程和 Agent 定义
document_parser.py：docx 和 md 文档解析模块
excel_exporter.py：Excel 测试用例导出模块
prompts.py：所有 LLM 提示词模板
app.py：Streamlit 前端界面
.env：环境变量配置
验收标准：项目根目录下能看到这 6 个文件
Cursor 技巧：在 Cursor 中输入 "创建一个 Python 项目的文件结构，包含 main.py、document_parser.py 等 6 个文件"，让 AI 自动生成这些空文件
🎯 第一阶段：文档解析模块（30 分钟）
核心目标：实现两种格式文档的解析，输出统一的结构化数据
步骤 1：定义统一的数据结构
在document_parser.py中定义一个数据类，用于表示文档的章节结构
数据类应包含：章节级别、标题、正文内容、表格列表、子章节列表这几个字段
验收标准：数据类定义清晰，能完整表示一个文档的层级结构
Cursor 技巧：描述你需要的数据结构，让 Cursor 帮你生成 dataclass 定义
步骤 2：实现 Markdown 解析功能
在同一个文件中创建 Markdown 解析函数
函数输入：原始 Markdown 文本字符串
函数输出：你刚才定义的文档结构对象
核心功能：识别标题层级、提取正文内容、解析表格数据、构建树状结构
验收标准：输入一个包含标题、列表、表格的测试 Markdown 文本，能正确输出对应的结构化对象
Cursor 技巧：告诉 Cursor"使用 markdown-it-py 库解析 Markdown 文档，按标题层级生成树状结构，提取所有表格内容"
步骤 3：实现 Word (docx) 解析功能
在同一个文件中创建 docx 解析函数
函数输入：docx 文件路径
函数输出：和 Markdown 解析相同的文档结构对象
核心功能：根据段落样式识别标题级别、提取段落文本、解析 Word 表格
验收标准：输入一个真实的 docx 文件，能正确输出对应的结构化对象，表格内容完整
Cursor 技巧：告诉 Cursor"使用 python-docx 库解析 Word 文档，根据 heading 样式识别标题层级，提取所有表格，返回和 Markdown 解析相同的结构"
步骤 4：添加统一转换函数
创建一个工具函数，将文档结构对象转换为字典格式
方便后续传递给 LLM 进行处理
验收标准：能将文档对象转换为 JSON 格式的字典
🎯 第二阶段：LangGraph 基础流程搭建（40 分钟）
核心目标：搭建完整的无人工干预的测试用例生成流程
步骤 1：初始化基础组件
在main.py中导入所有必需的库
加载.env文件中的环境变量
初始化大模型客户端（指定模型名称和温度参数）
定义系统的状态字典，包含所有中间结果字段
验收标准：导入语句没有报错，能成功初始化大模型客户端
Cursor 技巧：告诉 Cursor"导入 LangChain、LangGraph、OpenAI 等相关库，加载环境变量，初始化 ChatOpenAI 客户端"
步骤 2：编写所有提示词模板
在prompts.py中创建 4 个核心提示词模板：
需求分析提示词
测试点提取提示词
测试大纲生成提示词
详细用例生成提示词
每个提示词都要明确输入、输出格式和要求
要求 LLM 输出 JSON 格式，方便后续解析
验收标准：4 个提示词模板都已编写完成，格式清晰，要求明确
Cursor 技巧：描述每个提示词的作用和输出要求，让 Cursor 帮你生成专业的提示词模板
步骤 3：实现所有基础 Agent 节点
在main.py中创建 5 个核心节点函数：
文档解析节点：调用 document_parser 模块
需求分析节点：调用大模型生成需求分析报告
测试点提取节点：调用大模型提取分类测试点
大纲生成节点：调用大模型生成三级测试大纲
用例生成节点：调用大模型生成详细测试用例
每个节点函数输入是当前状态，输出是要更新的状态字段
验收标准：5 个节点函数都已定义完成，没有语法错误
Cursor 技巧：写好每个函数的注释和参数，让 Cursor 帮你实现函数内部逻辑
步骤 4：搭建并测试基础流程
创建 StateGraph 实例
添加所有 5 个节点
按顺序连接节点，形成线性流程
添加 Checkpointer 支持状态持久化
编译生成可运行的图
用一个简单的测试文档运行整个流程
验收标准：运行 main.py 能完整执行所有步骤，最终输出测试用例列表
Cursor 技巧：告诉 Cursor"使用 LangGraph 构建一个线性工作流，包含文档解析、需求分析、测试点提取、大纲生成、用例生成这 5 个节点"
🎯 第三阶段：人机交互点实现（50 分钟）
核心目标：添加两个审核节点和条件边，实现中断和恢复功能
步骤 1：添加人工审核节点
在main.py中创建两个审核节点函数：大纲审核节点和用例审核节点
每个审核节点都使用 interrupt () 函数暂停流程
中断时返回当前需要审核的内容和提示信息
验收标准：流程运行到审核节点时会自动暂停，返回审核内容
Cursor 技巧：告诉 Cursor"使用 LangGraph 的 interrupt 功能实现人工审核节点，暂停流程等待用户输入"
步骤 2：添加条件路由函数
创建两个路由函数：大纲审核路由和用例审核路由
每个路由函数根据人工反馈的 approved 字段决定流程走向
审核通过则继续下一步，不通过则回退到上一个生成节点
验收标准：路由函数能根据不同的输入返回正确的下一个节点名称
Cursor 技巧：告诉 Cursor"创建两个条件路由函数，根据人工反馈的 approved 字段决定是继续流程还是回退重新生成"
步骤 3：更新流程定义
向图中添加两个审核节点
修改原来的边连接，将生成节点连接到对应的审核节点
添加条件边，将审核节点连接到对应的路由函数
修改大纲生成和用例生成节点，使其支持基于人工反馈重新生成
验收标准：流程能正确在审核节点暂停，人工输入通过后继续，输入不通过后回退重新生成
Cursor 技巧：告诉 Cursor"更新 LangGraph 的流程定义，添加条件边连接审核节点和路由函数"
🎯 第四阶段：Excel 导出模块（30 分钟）
核心目标：实现带样式的 Excel 测试用例导出功能
步骤 1：实现基础 Excel 导出
在excel_exporter.py中创建导出函数
函数输入：测试用例列表和输出文件路径
函数输出：生成的 Excel 文件路径
核心功能：创建工作簿和工作表，写入表头和测试用例数据
验收标准：输入测试用例列表能生成一个 Excel 文件，包含所有测试用例数据
Cursor 技巧：告诉 Cursor"使用 openpyxl 库将测试用例列表导出为 Excel 文件，包含用例 ID、模块、功能点等标准字段"
步骤 2：添加 Excel 样式美化
设置表头样式：加粗、居中、添加边框
设置数据单元格样式：左对齐、自动换行、添加边框
自动调整列宽，确保内容完整显示
设置合适的行高
验收标准：生成的 Excel 文件样式美观，无需手动调整格式即可直接使用
Cursor 技巧：告诉 Cursor"为 Excel 文件添加样式，包括表头加粗、单元格边框、自动换行和列宽调整"
步骤 3：添加 Excel 导出节点
在main.py中创建 Excel 导出节点函数
调用 excel_exporter 模块生成 Excel 文件
将生成的文件路径保存到状态中
将导出节点添加到流程的最后一步
验收标准：流程完成后能自动生成 Excel 文件并保存到指定路径
🎯 第五阶段：Streamlit 前端集成（40 分钟）
核心目标：实现一个完整的 Web 界面，支持全流程操作
步骤 1：初始化 Streamlit 应用
在app.py中导入 Streamlit 和其他必需的库
配置页面标题和布局
初始化会话状态，用于存储当前步骤和线程 ID
验收标准：运行streamlit run app.py能打开一个空白的 Web 页面
步骤 2：实现文档上传页面
添加文件上传组件，限制只能上传 docx 和 md 格式
添加 "开始生成" 按钮
点击按钮后读取上传的文件内容
启动 LangGraph 流程，跳转到大纲审核页面
验收标准：能成功上传文档并启动生成流程
Cursor 技巧：告诉 Cursor"使用 Streamlit 创建一个文件上传页面，支持 docx 和 md 格式，上传后启动 LangGraph 流程"
步骤 3：实现大纲审核页面
从 LangGraph 中获取当前状态
展示 AI 生成的测试大纲
添加编辑区域，允许用户修改大纲
添加 "通过大纲" 和 "修改并重新生成" 两个按钮
点击按钮后恢复 LangGraph 流程，传递人工反馈
验收标准：能看到生成的大纲，修改后能重新生成，通过后能进入用例审核页面
步骤 4：实现用例审核和下载页面
用同样的方式实现用例审核页面
使用表格形式展示测试用例，更直观
实现下载页面，提供 Excel 文件下载按钮
添加 "生成新的测试用例" 按钮，重置整个流程
验收标准：能完整走一遍从上传到下载的全流程
🎯 第六阶段：优化和完善（30 分钟）
核心目标：修复 bug，提升体验，提高生成质量
步骤 1：优化提示词质量
为每个提示词添加更详细的约束和示例
明确要求 LLM 输出严格的 JSON 格式
添加异常场景和边界条件的生成要求
步骤 2：添加错误处理
在文档解析部分添加异常捕获
在 LLM 调用部分添加重试机制
在 Excel 导出部分添加目录创建逻辑
步骤 3：完善前端体验
添加加载动画和进度提示
优化页面布局，提高可读性
添加错误提示信息
步骤 4：清理临时文件
在流程结束后自动删除上传的临时文件
定期清理旧的生成文件
步骤 5：添加日志记录
添加简单的日志记录功能
记录每个步骤的执行时间和结果
🎯 第七阶段：最终测试和打包（20 分钟）
步骤 1：全流程测试
用一个真实的需求文档完整测试整个流程
测试 "修改并重新生成" 功能
检查生成的 Excel 文件格式和内容是否正确
步骤 2：创建启动脚本
创建一个简单的启动脚本，一键启动 Streamlit 应用
编写 README 文件，说明如何使用系统
步骤 3：Docker 打包（可选）
创建 Dockerfile
创建 docker-compose.yml 文件
测试 Docker 镜像是否能正常运行
✨ Cursor Vibe Coding 黄金法则
注释先行：先写函数注释和功能描述，再让 Cursor 实现具体逻辑
小步迭代：每写完一个函数就立即测试，不要一次写太多
善用 Ctrl+K：选中代码片段，按 Ctrl+K 描述你想要的修改
利用上下文：Cursor 会自动理解整个项目的代码，你只需要描述意图
遇到问题直接问：代码运行出错时，直接把错误信息粘贴给 Cursor，让它帮你修复