基于 LangChain / LangGraph 的多 Agent AI 测试用例生成系统（精简版）
仅支持：docx/markdown 文档输入 → Excel 测试用例输出
一、系统概述
1.1 核心目标
构建轻量、可落地、人机协作的 AI 测试用例生成 MVP 系统，仅处理 Word (docx) 和 Markdown (md) 格式的需求文档，自动化生成符合企业标准的 Excel 格式可执行测试用例，覆盖功能、异常、边界三大核心测试维度。
1.2 核心价值
精准文档解析：专门优化 docx/md 格式解析，完美保留标题层级、列表、表格、代码块等结构化信息
多 Agent 专业分工：模拟真实测试工程师工作流，分阶段完成需求理解→测试设计→用例生成
Excel 原生输出：直接生成带样式、可编辑、符合企业模板的 Excel 测试用例，无需二次格式调整
可复用能力沉淀：内置通用测试设计方法论和用例模板，支持自定义公司标准
极简人机协作：仅保留大纲和用例两个关键确认节点，平衡自动化效率与生成质量
二、核心流程设计
保留 7 个核心环节 + 2 个人机交互节点，全流程聚焦 docx/md→Excel 转化
plaintext
需求文档输入(.docx/.md)
    ↓
文档解析Agent
    ↓
需求分析Agent
    ↓
测试点提取Agent
    ↓
大纲生成Agent
    ↓
人工确认/修改大纲 ✋
    ↓
用例生成Agent
    ↓
人工确认/修改用例 ✋
    ↓
Excel测试用例输出(.xlsx)
各环节详细说明
表格
环节	输入	核心输出	关键能力
文档解析	.docx/.md 需求文档	带层级结构的结构化文本（章节→子章节→要点→表格）	docx/md 原生解析、标题层级识别、表格结构化提取、代码块保留
需求分析	结构化文档 + 业务知识库	需求分析报告（核心功能、业务流程、依赖关系、隐含需求）	RAG 检索增强、业务逻辑梳理、隐含需求挖掘
测试点提取	需求分析报告	分类测试点列表（正常功能 / 异常场景 / 边界条件）	等价类划分、边界值分析、错误推测法
大纲生成	测试点列表	三级结构化测试大纲（模块→功能点→测试场景）	大纲模板匹配、层级自动组织、场景分类
大纲微调	原始大纲 + 人工修改意见	优化后的最终测试大纲	差异对比、局部重新生成、增量更新
用例生成	最终大纲 + Excel 用例模板	标准化详细测试用例	模板字段自动填充、测试步骤分步生成、预期结果精准描述
Excel 输出	最终测试用例	带样式的 Excel 测试用例文件	模板渲染、列宽自动调整、样式美化、多工作表支持
三、技术架构设计
极简分层架构，砍掉非必要模块，最大化开发效率
plaintext
┌─────────────────────────────────────────────────┐
│                 用户交互层                      │
│  Streamlit前端（文档上传/大纲编辑/Excel下载）  │
│  FastAPI后端API（供后续集成其他系统）          │
└─────────────────────┬─────────────────────────┘
                       │
┌─────────────────────▼─────────────────────────┐
│               多Agent协调层                   │
│  LangGraph编排：状态管理、条件路由、人工暂停  │
│  包含Agent：文档解析/需求分析/测试点提取/大纲生成/用例生成 │
└─────────────────────┬─────────────────────────┘
                       │
┌─────────────────────┼─────────────────────────┐
│                 核心能力层                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ RAG引擎  │  │ Skill库  │  │ 工具集   │    │
│  │(FAISS)   │  │(测试技能)│  │(Excel生成)│    │
│  └──────────┘  └──────────┘  └──────────┘    │
└─────────────────────┬─────────────────────────┘
                       │
┌─────────────────────▼─────────────────────────┐
│                 数据存储层                    │
│  SQLite（历史记录）、本地文件系统（文档/Excel）│
└───────────────────────────────────────────────┘
四、核心模块设计
1. 文档解析模块（仅支持 docx/md）
功能：精准解析 docx 和 md 文档，完整保留结构化信息
关键实现：
Markdown 解析：使用mistune/markdown-it-py，完美支持标题、列表、表格、代码块、引用
Word (docx) 解析：使用python-docx，提取段落文本、标题样式、表格内容，自动识别章节层级
统一结构化输出：将两种格式文档转换为统一的 JSON 结构，包含章节ID、标题、层级、内容、子章节、表格字段
预处理优化：自动去除文档中的空行、冗余格式、页眉页脚等无关信息
2. RAG 引擎模块
功能：结合业务知识库提升需求理解准确性
关键实现：
向量存储：仅使用 FAISS（轻量开源，无需额外部署，完美适配 MVP 阶段）
文本分割：按语义分割（RecursiveCharacterTextSplitter），保留段落完整性
检索策略：混合检索（BM25 关键词 + 向量相似度），提升检索准确率
知识库：仅保留核心三类
通用测试方法论库
公司业务规则库
历史优质测试用例库
3. 多 Agent 协调模块
功能：编排测试用例生成全流程，支持人工干预
关键实现：
LangGraph 状态定义：
python
运行
class TestCaseState(TypedDict):
    document: str  # 原始文档内容
    structured_doc: dict  # 结构化文档
    requirement_analysis: str  # 需求分析报告
    test_points: list[dict]  # 测试点列表
    test_outline: list[dict]  # 测试大纲
    modified_outline: list[dict]  # 人工修改后的大纲
    test_cases: list[dict]  # 生成的测试用例
    modified_test_cases: list[dict]  # 人工修改后的用例
流程控制：线性流程 + 两个人工暂停节点，简单可靠
状态持久化：自动保存每个环节的中间结果，支持流程中断后继续
4. Skill 库模块
功能：沉淀可复用的测试设计原子能力
核心 Skill：
需求解析类：结构化需求提取、业务流程梳理
测试设计类：正常测试点提取、异常场景生成、边界条件挖掘
用例生成类：Excel 模板解析、用例字段填充、测试步骤生成
工具类：Excel 文件生成、格式转换、差异对比
5. Excel 生成模块（新增核心模块）
功能：将结构化测试用例转换为带样式的 Excel 文件
关键实现：
模板驱动：支持自定义 Excel 用例模板，可配置列名、顺序、宽度、样式
核心工具：openpyxl（生成带样式的 Excel）+ pandas（数据处理）
标准字段支持：用例 ID、模块、功能点、测试场景、前置条件、测试步骤、预期结果、优先级、测试类型
样式美化：自动调整列宽、设置表头样式、添加边框、支持单元格换行
多工作表支持：可按模块拆分生成多个工作表，或合并到一个工作表
五、技术选型（极简 MVP 版，100% 可快速落地）
1. 大模型（LLM）
表格
选型	适用场景	选型理由
GPT-4o / Claude 3.5 Sonnet	核心推理（需求分析、测试设计、用例生成）	推理能力强，擅长文本理解和结构化输出，成本适中
通义千问 3.5 / 豆包 4.0	国内替代	数据合规，响应速度快，支持私有化部署
2. 核心框架
表格
选型	用途	选型理由
LangChain	核心开发框架	生态成熟，集成了文档加载器、向量数据库、LLM 调用
LangGraph	多 Agent 流程编排	专门用于构建带状态和人工干预的工作流，完美匹配本系统需求
3. 文档处理工具
表格
工具	处理格式	选型理由
python-docx	.docx	原生支持 Word 文档解析，可提取标题样式和表格
markdown-it-py	.md	解析速度快，支持完整的 Markdown 语法
4. Excel 生成工具
表格
工具	用途	选型理由
openpyxl	生成带样式的 Excel 文件	支持 xlsx 格式，可设置单元格样式、列宽、边框等
pandas	测试用例数据处理	方便进行数据转换和批量操作
5. Web 与 API 框架
表格
选型	用途	选型理由
Streamlit	前端界面	纯 Python 开发，无需前端知识，1 天即可搭建可用原型
FastAPI	后端 API	性能高，自动生成 API 文档，支持异步
6. 数据存储
表格
选型	存储内容	选型理由
SQLite	历史记录、用户信息	无需额外部署，轻量易用
本地文件系统	原始文档、生成的 Excel 文件	简单直接，适合 MVP 阶段
7. 部署工具
表格
选型	用途	选型理由
Docker	环境一致性	一键打包所有依赖，避免 "在我电脑上能运行" 问题
Docker Compose	本地部署	单命令启动整个系统（前端 + 后端 + 向量数据库）
六、Excel 输出示例（标准格式）    




参考目录
ai-test-case-generator/
├── .env                      # 真实环境变量（不提交）
├── .env.example              # 环境变量模板（提交）
├── .gitignore                # Git忽略文件
├── requirements.txt          # 依赖列表
├── README.md                 # 项目说明
│
├── app.py                    # Streamlit前端入口（单文件）
├── main.py                   # LangGraph核心流程（单文件）
│
├── parsers/                  # 文档解析模块（文件夹，预留扩展）
│   ├── __init__.py
│   ├── markdown_parser.py    # Markdown解析
│   └── docx_parser.py        # Docx解析
│
├── exporters/                # Excel导出模块（文件夹，预留扩展）
│   ├── __init__.py
│   └── excel_exporter.py     # Excel导出
│
├── utils/                    # 工具函数（文件夹，放通用工具）
│   ├── __init__.py
│   └── helpers.py            # 辅助函数（如文件清理、日志等）
│
├── prompts/                  # 提示词模块（可选文件夹，提示词多时用）
│   └── prompts.py            # 所有LLM提示词
│
├── temp/                     # 临时文件目录（自动创建，不提交）
└── output/                   # 输出文件目录（自动创建，不提交）